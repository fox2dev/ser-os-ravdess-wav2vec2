# Python dependencies for SER_Wav2Vec2_OS_Optimized_Windows_GPU.ipynb
# Install PyTorch separately from the official selector:
# https://pytorch.org/get-started/locally/
# This repo keeps PyTorch outside requirements.txt because the wheel depends on
# the user's CUDA / CPU environment on Windows.

accelerate
filelock
imbalanced-learn
ipykernel
ipywidgets
kagglehub
librosa
matplotlib
numpy
pandas
psutil
scikit-learn
soundfile
tqdm
transformers
